@model IEnumerable<Course.Models.Test>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Unit.lesson)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Test_case)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Unit.lesson)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Test_case)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Test_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Test_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Test_id })
        </td>
    </tr>
}

</table>
